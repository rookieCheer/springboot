使用admin登陆  有所有的权限
使用 test登陆  点击没有权限的页面 没有权限的也面 权限拦截器 拦截 提示没有权限的页面
在另外一个浏览器登录 这个账号被挤下来

功能
   一个浏览器可以登录多个账号
   单系统  单一登陆
   全局异常 处理



思路
      登陆和权限校验     使用注解和拦截器      在权限拦截器中获取注解的值  对比数据库是否具有权限  有权限放行  没有跳转到没有权限页面
                                                登陆接口 登陆成功 将用户信息放入session中  访问别的接口是拦截器拦截 判断session中是否有用户信息 判断登陆
      一个浏览器可以登录多个账号          在session中放入用户信息的时候  key值使用每个账号都不一样的标识
      单系统  单一登陆    使用session监听器     在session添加的时候  判断准备好的map中是否有 这个session  没有 正常登陆   有  删除对应session



bug   1 当登陆正常 然后出现访问没有接口的路径时  权限拦截器 中
        Method method = ((HandlerMethod) handler).getMethod(); 类型转换异常

      2 在同一个浏览器中登陆同一个账号   两个账号都会下线,继续登陆不退出的话 还会是下线状态只有推出后在正常